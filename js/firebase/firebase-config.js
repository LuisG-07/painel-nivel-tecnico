// Configuração do Firebase (Web SDK) — SkillMatrix / GCK
// ─────────────────────────────────────────────────────────────────────────────
// Estes valores NÃO são segredo (a config web do Firebase é pública por design);
// a segurança vem das regras do Firestore (firestore.rules).
//
// Os valores abaixo são PLACEHOLDERS. Serão preenchidos automaticamente após
// você rodar `npx firebase login` e criar/escolher o projeto — o assistente
// puxa a config com `firebase apps:sdkconfig web` e atualiza este arquivo.
window.FIREBASE_CONFIG = {
  apiKey:            "__API_KEY__",
  authDomain:        "__AUTH_DOMAIN__",
  projectId:         "__PROJECT_ID__",
  storageBucket:     "__STORAGE_BUCKET__",
  messagingSenderId: "__MESSAGING_SENDER_ID__",
  appId:             "__APP_ID__"
};
